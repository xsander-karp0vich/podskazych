# Рантайм распознавания речи для установщика: точные версии, на которых он
# проверен (scripts/build-stt-runtime.ps1 сверяет с ними пакеты из venv).
#
# Это вход для lock-файла с хэшами, по которому CI ставит пакеты:
#   uv pip compile sidecar/requirements-stt.txt --generate-hashes --python-version 3.14 --python-platform x86_64-pc-windows-msvc -o sidecar/requirements-stt.lock.txt
# Поменяли версию здесь — пересоберите lock и прогоните build-stt-runtime.ps1.
#
# В отличие от requirements.txt здесь нет cuDNN и scipy: CTranslate2 распознаёт
# на CUDA без cuDNN, а scipy сайдкар не импортирует.

faster-whisper==1.2.1
# закреплён: на процессоре кодировщику подаётся окно короче 30 с (engines.py)
ctranslate2==4.8.2
onnxruntime==1.29.0
av==18.1.0
numpy==2.5.2
tokenizers==0.23.2
huggingface-hub==1.30.0
tqdm==4.70.0
colorama==0.4.6
filelock==3.32.5
fsspec==2026.7.0
packaging==26.3
pyyaml==6.0.3
typing-extensions==4.16.0
soxr==1.1.0
PyAudioWPatch==0.2.12.8
websockets==17.1

# только cuBLAS и cudart; nvrtc приходит зависимостью cuBLAS, в установщик не кладётся
nvidia-cublas-cu12==12.9.2.10
nvidia-cuda-runtime-cu12==12.9.79
nvidia-cuda-nvrtc-cu12==12.9.86

# зависимости huggingface_hub и onnxruntime, которые ставятся, но в рантайм не
# копируются (нужны для скачивания и инструментов, а не для распознавания)
anyio==4.15.1
certifi==2026.7.22
click==8.5.0
h11==0.16.0
hf-xet==1.6.0
httpcore==1.0.9
httpx==0.28.1
idna==3.19
flatbuffers==25.12.19
protobuf==7.36.1
